# Agent Worker Configuration
# Copy this file to .env and fill in your values

# Required: Anthropic API Key for Claude Code
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Custom base URL for Claude API (for proxy or alternative endpoints)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# Optional: Timezone for the container (default: UTC)
# TZ=America/New_York

# Optional: Agent execution timeout in seconds (default: 3600)
# AGENT_TIMEOUT=3600

# Optional: Remote Docker host (for distributed execution)
# DOCKER_HOST=tcp://remote-host:2375

# Optional: Default project path (default: current directory)
# PROJECT_PATH=/path/to/your/project

# Optional: Default agent type (default: claude)
# AGENT_TYPE=claude

# Optional: Enable debug logging (default: false)
# DEBUG=true
